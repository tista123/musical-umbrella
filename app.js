const heading =React.createElement('h1', null, 'Hello, World!');
const timepass= React.createElement('h2', null, 'This is a heading created by JS');
const hi = React.createElement('h3', null, 'This is a heading created by Tista');

const root =ReactDOM.createRoot(document.getElementById('root'));
const mad =ReactDOM.createRoot(document.getElementById('mad'));

root.render(timepass);

mad.render(h3) ;


